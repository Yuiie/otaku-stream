{% trans_default_domain 'FOSUserBundle' %}

<div class="fos_user_user_show">
    <p><FONT color="#E9A713" size="20px">{{ 'profile.show.username'|trans }}: {{ user.username }}</FONT></p>
    <p>{{ 'profile.show.email'|trans }}: {{ user.email }}</p>
</div>
